ktor {
    application {
        modules = [ com.compare.App.module ]
    }
}

server {
    connectors {
        listOf({
            type = "HTTP"
            host = "0.0.0.0"
            port = 8080
        })
    }
}